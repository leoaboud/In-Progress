/*
RESUMO DIDÁTICO:
Este script apresenta a estrutura fundamental de um programa na linguagem Go (Golang).
O código demonstra a declaração do pacote principal (package main), a importação
do pacote de formatação (fmt) e a definição da função main(), que serve como o
ponto de entrada obrigatório para qualquer executável. Através da função
fmt.Println(), o script realiza a saída básica de dados (strings) no console,
ilustrando a sintaxe limpa e organizada da linguagem.
*/

package main

import (
	"fmt"
)

func main() {
	// Imprime a mensagem padrão de boas-vindas ao console
	fmt.Println("Ola mundo!")
}
